name: date_time_extensions
description: "A dart package that extends some date time utilities from Jiffy."
version: 1.0.0
homepage:

environment:
  sdk: ">=3.3.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2
  jiffy: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
